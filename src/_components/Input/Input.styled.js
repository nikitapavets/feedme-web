import styled from 'styled-components';

import theme from '../../lib/theme';

export const Wrap = styled.div``;

export const InputText = styled.input`
  display: block;

  width: 100%;

  padding: 5px 8px;

  border: 0;
  border-bottom: 1px solid ${theme.brand};

  background: rgba(74, 112, 160, 0.25);
`;
